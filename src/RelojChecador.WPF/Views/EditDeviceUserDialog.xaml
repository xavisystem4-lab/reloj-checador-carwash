<Window x:Class="RelojChecador.WPF.Views.EditDeviceUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar usuario del reloj" Height="270" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="PIN (no editable)" FontWeight="SemiBold" Margin="0,0,0,2" />
            <TextBox x:Name="PinTextBox" Padding="4" IsReadOnly="True" Background="{DynamicResource BrushSurface}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Nombre *" FontWeight="SemiBold" Margin="0,0,0,2" />
            <TextBox x:Name="NameTextBox" Padding="4" />
        </StackPanel>

        <CheckBox Grid.Row="2" x:Name="IsEnabledCheckBox" Content="Usuario habilitado en el reloj" Margin="0,0,0,10" />

        <TextBlock Grid.Row="3" x:Name="ErrorTextBlock" Foreground="{DynamicResource BrushDanger}" TextWrapping="Wrap"
                    Visibility="Collapsed" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Click="OnCancelClick" IsCancel="True" />
            <Button Content="Guardar" Width="90" Style="{StaticResource PrimaryButton}" Click="OnSaveClick" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
