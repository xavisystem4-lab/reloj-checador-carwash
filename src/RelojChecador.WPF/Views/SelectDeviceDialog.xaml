<Window x:Class="RelojChecador.WPF.Views.SelectDeviceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Elegir dispositivo" Height="220" Width="380"
        MinHeight="200" MinWidth="360"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="¿A cuál reloj checador?" FontWeight="SemiBold" Margin="0,0,0,2" />
            <ComboBox x:Name="DeviceComboBox" Padding="4" DisplayMemberPath="Name" />
        </StackPanel>

        <TextBlock Grid.Row="1" x:Name="ErrorTextBlock" Foreground="{DynamicResource BrushDanger}" TextWrapping="Wrap"
                    Visibility="Collapsed" Margin="0,0,0,10" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Click="OnCancelClick" IsCancel="True" />
            <Button Content="Continuar" Width="100" Style="{StaticResource PrimaryButton}" Click="OnContinueClick" IsDefault="True" />
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>
